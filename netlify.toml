[build]
  publish = "dist"
  command = "npm run build"

# Normalize trailing slash -> no slash
[[redirects]]
  from = "/:splat/"
  to = "/:splat"
  status = 301
  force = true

# SPA fallback – for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
